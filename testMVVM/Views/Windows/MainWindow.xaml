<Window x:Class="testMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:testMVVM"
        xmlns:vm="clr-namespace:testMVVM.ViewModels"
        xmlns:cmd="clr-namespace:testMVVM.Infrastructure.Commands"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" Height="450" Width="800"
        MinHeight ="300" MinWidth="500"
        >
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Выход">
                    <MenuItem.Command>
                        <cmd:CloseApplicationCommand/>
                    </MenuItem.Command>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Помощь">
                <MenuItem Header="О программе"></MenuItem>
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem MaxHeight="150" MaxWidth="200">
                <TextBlock Text="{Binding Status}"></TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar Width="120" Height="14" Value = "80"></ProgressBar>
            </StatusBarItem>
        </StatusBar>
        <Grid Width="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*" MinWidth="150" MaxWidth="500"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>
            <TabControl Width= "Auto" Grid.Column ="1">
                <TabItem Header="Вкладка 1">
                    <Grid Background="#FFE5E5E5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="63*"/>
                            <RowDefinition Height="325*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="157*"/>
                            <ColumnDefinition Width="637*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle Grid.Column="0" Grid.Row="0" Fill="Blue" Stroke="Black" StrokeThickness="3" RadiusX="7" RadiusY ="5"></Rectangle>
                        <Ellipse Grid.Column="1" Grid.Row="0" Fill="Aqua" Stroke="Red" StrokeThickness="5" Width="Auto" Height="Auto"></Ellipse>
                        <GridSplitter Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" Width="4"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Вкладка 2">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
            </TabControl>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Left" Width="4"/>
        </Grid>
    </DockPanel>

</Window>
